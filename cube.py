class cube:
	def __init__(self, top, front, bottom, back, left, right):
		self.top = top
		self.front = front
		self.bottom = bottom
		self.back = back
		self.left = left
		self.right = right

	def print(self):
		